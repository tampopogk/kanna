# Delivery status — tasks, sessions and structured workflows

Coordinating parent: task `482a02db`. Specification: `docs/specs/tasks-sessions-structured-workflows.md` at `9a4198da8`.
This manifest is the parent's durable record of component children, their prerequisite commits, reviewed results and
integrated commits, so a resumed or revising parent can reconcile without duplicating children.

Child workflow: first wave (T0, T6, T8) ran `plan-build-review`, edited after its plan settled to
`plan[M] -> in progress[a]+commit -> review[M, final]` (no `pr`/`approve`). Owner direction 2026-09-23: later children
skip the child plan stage — create on `specialized-reviewers` (panel) or `single-reviewer` (UI/definition-only), building
straight from the card, and edit only the tail to `review[M, final]` with no `pr`. Publication stays in this parent's
single PR stage. The parent operates each child's plan and
final review gates as manager; children return reviewed local commits only.

| Task | Child id | Scope / current checkpoint | Base / prerequisites consumed | State | Reviewed commit(s) | Integrated as |
|---|---|---|---|---|---|---|
| T0 ledger bridge | `1dc3da40` | full card | `task-482a02db-3` @ `9a4198da8` | reviewed (round 4), integrated, closed | `c91542a2c`, `b18b06b29`, `976ecdf37`, `472068345` | merge commit below |
| T6 local artifacts | `ed3533d4` | first increment (publish/open by tree id); result binding after T0.review | `task-482a02db-3` @ `9a4198da8` | increment 1 reviewed + integrated; closed (checkpoint 2 moved to T6b) | `dc9c09bfa` | `20fd89157` |
| T8 provenance | `d06e30e1` | first increment (channel identity capture); account boundary split to T8b | `task-482a02db-3` @ `9a4198da8` | increment 1 reviewed (round 2), integrated, closed | `65e1943e5`, `7c06a0de4` | `a609c5aec` |
| T8b same-account boundary | `216cf1c2` | account-boundary enforcement + T8 follow-ups (build-first, specialized-reviewers; build gate manual) | parent `e679678d0` (T6+T8 integrated) | reviewed (round 2), integrated, closed | `7863ff1f0`, `a00800e67` | `bab127907` |
| T1 workflow contract | `e3ef840c` | first increment + card | parent `cc8125f85` (T0 472068345 + T6/T8/T8b), merged `df6ee9040` | reviewed (round 1), integrated, closed | `0c4780b56`, `257e08f5a` | T1 merge commit |
| T2 stage workspaces | `cb28da39` | first increment + card | parent `cc8125f85` (T0 472068345 + T6/T8/T8b), merged `b1334e528` | reviewed (round 6), integrated, closed | `742b8b0d6` … `e9d863ab0` | T2 merge commit |
| T6b artifact result refs + retention | `d6fb5ce0` | T6 checkpoint 2 + T6 follow-ups | parent `cc8125f85` (+ merged `918d8c8fa`) | reviewed (round 2), integrated, closed | `8ba365b24`, `cc04f99ba`, `fd6f7e2e9` | T6b merge commit |
| T8c ledger channel identity | `7049470b` | T8 ledger fill (single reviewer, sonnet) | parent `cc8125f85` | reviewed, integrated, closed | `364fe7578` | merge after `918d8c8fa` |
| T12b artifact remote controls + §14 | `f73ff638` | remote controls, mobile recording, T12 follow-ups, §14 walkthrough doc + device checklist (human checks outstanding) | parent `8885864f6` (T7 373127ab4, T12 f38895884) | reviewed (round 3), integrated, closed; human checks outstanding | `517e55f60` … `48162c567` | T12b merge commit |
| Fix: stage-complete --artifacts | `72b57433` | correction for T6b's missing typed CLI option (single reviewer, sonnet) | parent `c2620957b` | reviewed, integrated, closed | `87f633358` | `4c92ead01` |
| T3 commit transitions / roleless gates | `f7fc3a78` | first increment + card | parent `b70608f54` (T1 257e08f5a, T2 e9d863ab0) | reviewed (round 2), integrated, closed | `e599edb75`, `2f91f9ad2` | T3 merge commit |
| T4 stage dependency edges | `c6db4bb4` | first increment + card | parent `b70608f54` (T1 257e08f5a, T2 e9d863ab0), merged `c05999e0f` (T3) | reviewed (round 3), integrated, closed | `1d5c9fc8a` … `19f785fdb` | T4 merge commit |
| T5 subtask joins | `8769b94e` | first increment + card (panel) | parent `3b11ea341` (T4 19f785fdb), merged `7acc9197f` (T14) | reviewed (round 3), integrated, closed | `f73b8e057` … `c740589e2` | T5 merge commit |
| T7 artifact remote | `a07d574f` | increment 1: two homes publish/fetch/comment via one Git remote (CLI/API) | parent `01f5c8972` (T6 dc9c09bfa, T8 7c06a0de4, T8b a00800e67) | reviewed (round 3), integrated, closed | `1019f2221`, `225525c97`, `373127ab4` | merge after `6ea23d959` |
| T9 transfer | `6cc08860` | first increment + card (panel) | parent `42ba8ed70` (T1–T5, T6/T6b/T7, T8/T8b/T8c, T14) | in progress | — | — |
| T10 definitions | `0e2062af` | first increment only: target frontmatter in loaders, preamble carries waiting/binding/exits, one mechanical workflow converted, format checks (panel incl. release; sonnet); lineup conversion → T10b | parent `42ba8ed70` | reviewed (round 2), integrated, closed | `d84a61b1e`, `f4f1e855b` | T10 merge commit |
| T11a latest result UI | `c719f2d9` | T11 increment 1: latest result/exit/artifact refs on desktop+mobile (single reviewer, sonnet); session name after T2 | parent `e69577ba6`, merged `b70608f54` | reviewed (round 2), integrated, closed; visual glance outstanding | `7e4b16a8b`, `89b085820`, `e5376f1f8` | T11a merge commit |
| T11b session/gate/dependency UI | `43be5d42` | T11 remainder (single reviewer, sonnet) | parent `3b11ea341` (T2, T3, T4) | in progress | — | — |
| T12 artifact viewer | `6d3d22f8` | increment 1: local open-by-hash desktop/mobile, anchors, isolation; remote + §14 walkthrough → T12b | parent `01f5c8972` (T6 dc9c09bfa, T8/T8b) | reviewed (round 4), integrated, closed | `beffdbe8b` … `f38895884` | T12 merge commit |
| T13a inventory + offline rebuild | `e3991858` | T13 increment 1: authority inventory, projector, fixture round trip (panel) | parent `e69577ba6`, merged `b70608f54` | reviewed (round 1), integrated, closed | `0d4a8a1c7`, `710ecb046`, `1c20d3622` | T13a merge commit |
| T13 remainder (cutover) | — | — | needs T1–T12, T14 review | not created | — | — |
| T14 release workflow | `6b069969` | first increment + card (panel incl. release/security) | parent `c05999e0f` (T3 2f91f9ad2, T8/T8b) | reviewed (round 3), integrated, closed | `0145b8de3`, `16c0065db`, `4d2feac1e`, `c9a782a9b` | T14 merge commit |

## Log

- 2026-09-23: first wave created from parent tip `9a4198da8`.
- 2026-09-23: T8 plan accepted (channel-identity type in `mutation_provenance.rs`; additive SQL provenance on runs/inputs/events); workflow edited to qa-dispatcher final manual review; advanced to build.
- 2026-09-23: T6 plan accepted (bare artifacts.git outside worktree, parentless retained content refs, metadata-only ref, loopback read-only preview listener, six catalog tools); same workflow edit; advanced to build.
- 2026-09-23: T0 plan accepted (store at ~/.kanna/repos/<repo-id>/tasks/<task-id>/, task.json snapshot + immutable ledger/<seq>-*.{md,json}, SQL outbox in originating txn, publication barrier before announce/dispatch, KANNA_TASK_LEDGER_PATH + triggering result in preamble, reserved declared_role/channel_identity/artifacts); same workflow edit; advanced to build.
- 2026-09-23: T8 review round 1 failed (security: LAN machine-invoke identity must record the account the bearer secret was verified under, not a fresh dispatch-time read); migration/compat passed; 181 scoped tests pass. Auto revision 1/5 to build. Non-blocking follow-ups: forged-header extractor coverage beyond loopback; tolerant unknown-kind ChannelIdentity decoding before a typed peer consumer exists.
- Environment note: specialty reviewers could not compile on Darwin 27 (zig libcxx break); T8's dispatcher ran scoped tests via a MacOSX26.5 xcrun shim.
- 2026-09-23: T6 increment 1 passed panel review at `dc9c09bfa` (compat/perf pass; security/concurrency/migration findings judged non-blocking; 65 scoped server tests + 55 catalog + 29 TS config + CLI round trip). Merged into parent as `20fd89157` (tree = reviewed commit + manifest only). T6 stays open for checkpoint 2 (result binding, retention) after T0.review; carry these follow-ups into it: case-insensitive `.git` component check; bounded preview drain after close/expiry; persisted monotonic version sequence instead of wall clock; preview session cap; navigation-to-control-port browser test; document config.local downgrade rejection of `artifacts`.
- 2026-09-23: T8 increment 1 passed panel review round 2 at `7c06a0de4` (security fix: LAN invoke records the account verified with the secret; 19 scoped tests). Merged as `a609c5aec` alongside T6; shared files http_api.rs, router.rs, http_api/tests.rs, main.rs, task_creator/mod.rs auto-merged — combined build/test in progress. Contract note: dispatch_authenticated_lan_http_invoke gained verified_account_uid. Account-boundary enforcement split into follow-on child T8b (separate contract), plus T8 follow-ups; ledger channel_identity fill waits for T0.review.
- 2026-09-23: combined T6+T8 check at `a609c5aec` (xcrun MacOSX26.5 shim): 239 scoped tests passed, 0 failed (artifact, local_config, every_registered_http_route, mutation_provenance, lan_listener, peer_legacy, lan_machine, http_api actions/input/workflow_switch); clippy -D warnings clean except 3 pre-existing `nonminimal_bool` errors in http_api/desktop_views.rs, unchanged since base 9a4198da8 (not in delivery scope). T8 review gate operated and task closed.
- 2026-09-23: T8b created from parent tip `e679678d0`, claude opus medium, tail edited to qa-dispatcher final manual review.
- 2026-09-23: T0 review round 1 failed at `c91542a2c`: concurrency — 3 blocking findings in outbox/continuation recovery; migration and compat passed; 354 scoped Rust + 46 prompt-builder tests pass. Auto revision 1/5 to build.
- 2026-09-23: T8b build committed `7863ff1f0` (account_boundary module; relay/LAN-machine/sealed-peer same-account enforcement; legacy pin diagnostics + re-pair; federation unreachable owner → 503; tolerant ChannelIdentity decode). 178 scoped tests pass; 4 full-suite failures reported as base/TMPDIR artifacts (to be confirmed in review). Advanced to panel review. Out-of-scope follow-up: daemon socket mode follows process umask and daemon input has no uid check (crates/daemon/src/socket.rs).
- 2026-09-23: T8b review round 1 failed: compat — stream-client does not treat `peer_account_boundary` as a connection refusal (silent reconnect loop instead of the re-enrollment diagnostic). Security ABA race and pre-existing plaintext claim error judged non-blocking; 325 scoped tests pass. Auto revision 1/5. Follow-ups: routing-generation snapshot for relay_confirms_sibling; structured claim_pairing_offer error body; document one-sided re-enrollment; old-reader/new-writer peer_trust fixture.
- 2026-09-23: T8b passed round 2 at `a00800e67` (stream-client treats peer_account_boundary as refusal; 86 stream-client tests, tsc clean; 325 scoped server tests from round 1). Merged as `bab127907` (tree = reviewed + manifest). T8b closed. Remaining T8 work: write channel_identity into T0's ledger envelope after T0.review (parent-owned small step or T0 follow-up).
- 2026-09-23: T7 (`a07d574f`) and T12 (`6d3d22f8`) created from parent `01f5c8972` after T6/T8/T8b review; build-first on specialized-reviewers, claude opus medium, tails edited to panel final manual review.
- 2026-09-23: T0 review round 2 at `b18b06b29`: round-1 findings fixed (180 tests green); new blocking concurrency finding — human-origin revision continuation stores runId null, fence treats null as wildcard, stale revision could dispatch over a resumed session. Revision 2/5.
- 2026-09-23: T0 review round 3 at `976ecdf37`: round-2 fix confirmed, but absent runId now means never-owed and strands a runless revision after deferred flush/restart. Revision 3/5. Parent sent guidance: give runless continuations an explicit fence identity (operation id + accepted generation), owed until settled, stale only when superseded; one test matrix covering both sides across flush and restart.
- 2026-09-23: T7 build committed `1019f2221` (artifacts.remote config; git-executable push/fetch with user credentials; refs/kanna/artifacts/shared/content/<tree>/<commit> + records/<tree>/<kind>/<id>, one immutable ref per record; private incoming namespace with validation; kanna_push_artifact/kanna_fetch_artifact). Two-server MCP+CLI test with remote inventory sentinels passes. Known limits: bare remote can't prevent force-push by writers; no byte cap before fetch validation. Advanced to panel review.
- 2026-09-23: T12 build committed `beffdbe8b`, verdict unverified: native mobile ArtifactViewer screen and WebView navigation refusal not visually checked (simulator cannot pair); mobile document isolation checked in simulator Safari; desktop mock E2E in real Tauri webview passed incl. hostile probe. Contract changes: GET artifacts/{id}/files route (1 MiB cap), preview CSP frame-ancestors widened to desktop webview origins incl. http://localhost:* and 127.0.0.1:*, ArtifactFileContent type, desktop 'artifact' tab. Mobile has no comment/decision recording UI yet. Parent advanced to panel review; the physical-device mobile check is carried to T12 checkpoint 2 (§14 human walkthrough).
- 2026-09-23: T12 review round 1 failed: security — Android navigation refusal fail-open; isolation tests cannot fail when the probe succeeds; UI — mobile Previous to a missing version strands the user. Revision 1/5.
- 2026-09-23: T0 passed panel review round 4 at `472068345` (continuation fence by operation id + run generation; 8-cell matrix). Merged into parent after T6/T8/T8b. Parent-resolved conflicts: migrations — kept `095_mutation_provenance`, renumbered T0's to `096_task_ledger_bridge` (latest-migration assert updated); `task_input.rs` — T8 channel argument into T0's record+flush; `task_actions.rs` — T0's deferred completion dispatch/continuation structure with T8 provenance: engine-driven completion transitions and continuation-recovered revisions record ChannelIdentity::Server, request-driven revisions record the caller's channel, park path threads provenance; T0 tests updated for T8 signatures; one T8 test now polls for `run.started` since T0 releases it after publication.
- Combined verification (T0+T6+T8+T8b): full `kanna-server` bin suite 2140 passed, 1 failed (`ksp::tests::bounded_request_saturation_keeps_terminal_input_responsive`); control at base `9a4198da8` fails identically ("timed out waiting for frame") in 4 of 5 runs at load 15–19 → pre-existing, load-sensitive. packages/core prompt-builder + config vitest 120/120, tsc clean.
- 2026-09-23: T7 review round 1 failed (3 blocking, reproduced): push follows remote-imported `previous` links into never-shared local artifacts (disclosure); git timeout reaps only git so descendants holding pipes hang the request; racing identical record pushes report a false conflict. Revision 1/5. Owner decision to raise at T7 integration: committed repo config can set `artifacts.remote`, so a cloned repo chooses where pushes go.
- 2026-09-23: from parent `cc8125f85`: created T1 `e3ef840c`, T2 `cb28da39` (panel), T6b `d6fb5ce0` (panel), T8c `7049470b` (single reviewer, sonnet). T0 and T6 closed via their review gates.
- 2026-09-23: T12 review round 2 at `227be857e`: security passes (sandboxed srcdoc frame) but in-tree kanna-artifact: links no longer reach the WebKit navigation callback (UI fail, reproduced on real WKWebView). Revision 2/5. Parent guidance: host document handles validated in-tree postMessage requests; test navigation and isolation together.
- 2026-09-23: T7 review round 2 at `225525c97`: bounded git and push race confirmed fixed; new blocking — previous_links_with_provenance reads metadata tip twice without the lock, so a concurrent fetch import can reopen the planted-previous-link disclosure. Non-blocking: setsid descendant leaks one reader thread + 2 fds until exit. Revision 2/5.
- 2026-09-23: T8c build `364fe7578` (real declared_role + ChannelIdentity in every ledger entry kind; backfill/imports tagged unknown); advanced to single review.
- 2026-09-23: T12 review round 3 at `991759502`: security pass; in-tree navigation fixed on WebKit; perf/UI fail — buildArtifactSite eagerly renders every reachable page before first display. Revision 3/5.
- 2026-09-23: T7 passed panel review round 3 at `373127ab4` (single-snapshot provenance read under the repository lock). Merged cleanly into parent (after T0). Combined check: kanna-server artifact/route-audit/local_config/task_store 92 passed; tool-catalog all pass; kanna-mcp artifact_sharing (two real servers, MCP + CLI) passed after building kanna-server. T7 closed. Non-blocking follow-ups carried: setsid descendant drain leak; no byte cap before fetch validation; redact() misses userinfo with '/'; incoming/<nonce> not swept after crash; per-link fetch deadline; golden record-commit and pre-T7 repo fixtures. Owner decision still open: committed repo config may set artifacts.remote.
- 2026-09-23: T6b build `8ba365b24` (result artifact references bound in the ledger; retention sweep with gc --prune=now under the repo flock; T6 follow-ups incl. sandboxed shell page). Before review, T6b is asked to merge the parent tip containing T7 and make T7 fetch/import/push hold the flock against the sweep.
- 2026-09-23: T8c passed single review at `364fe7578`; merged cleanly after T7. Combined check: 260 passed (task_store, input, actions, workflow_switch, mutation_provenance, artifact). T8c closed. Follow-ups: envelope doc should list the "human" revision role; HTTP-level assertion that complete-stage results carry declared_role "agent"; engine-observed results (exit without verdict, spawn failure) write no ledger result entry (T0 scope, carry to T13 inventory).
- 2026-09-23: T12 passed panel review round 4 at `f38895884` (host-originated in-tree navigation with sandboxed allow-scripts frame, no bridge; on-demand page reads; real WKWebView harness by reviewers). Merged into parent; resolved router.rs import list and core artifacts/types.ts as unions of T7 + T12 additions. Combined check: server artifact + route audit 63 passed; core tsc; desktop ArtifactViewer 10/10 + vue-tsc; mobile viewer/document/navigator/TaskScreen vitest 156/156 + tsc. Unverified (carried to T12b): real react-native-webview on iOS device and any Android WebView. Follow-ups: desktop tab persistence restores original id; committed DOM-click-to-callback test; Android 250 ms fail-open flash; stronger desktop fetch-probe assertion; cooperative cancellation overlap; i18n for artifact strings; host-open remount cost. T12 closed; checkpoint 2 moves to T12b.
- 2026-09-23: T1 build `0c4780b56` (routing: exits; per-destination stage budgets; loop exits via T0 REVISION continuation; exit fields in result + transition entries; remaining-plan replacement without recipes; migration 097_stage_exit_budget). Asked to merge parent `df6ee9040` (T7/T8c/T12) before review and reconcile T8c provenance in complete_stage.
- 2026-09-23: T12b `f73ff638` created from parent `8885864f6`.
- 2026-09-23: T6b merged parent `918d8c8fa` (T7) as `cc04f99ba`: fetch holds the repo flock from before git fetch until import refs exist; push holds it for the whole push (slow remote can block local publish/sweep up to 300 s); sweep clears stale incoming/ refs; received content gets local receipts and follows local retention from receipt; T7 record-id validator accepts sequence ids. Lock tests fail with locks removed. 51 artifact tests + two-server sharing pass. Advanced to panel review.
- 2026-09-23: T2 build `742b8b0d6` (revisit reuses retained directory with a new branch; persisted per-task branch counter seeded above refs/dirs/records; fork from triggering result SHA; dirty/diverged preserved and reported; unified session start; session_ref extended additively; TaskLatestRun.session). Migration order fixed by parent: T1 `097_stage_exit_budget`, T2 renumbered to `098_stage_workspaces`. T2 asked to merge parent `b1334e528` before review. Known limit: commits after a result in a manual stage are not carried forward until T3 exit_commit.
- 2026-09-23: T6b review round 1 failed: migration — two blocking findings in retention/sharing interplay; non-blocking: sweep-vs-reopen race, sequence ids vs unmodified-T7 peers (none shipped), preview shell/CSP. Security pass. Revision 1/5.
- 2026-09-23: T2 renumbered to 098 (`8a9a74bf3`) and merged parent `b1334e528` (`de8872965`); 706 tests pass post-merge; desktop 45/45. Advanced to panel review.
- 2026-09-23: T12b build `517e55f60`, verdict unverified (human/device checks outstanding): automated §14 data flow through the real router passes (two homes + bare remote, comment/decision round trip, B cannot advance A's task, A's owner can); desktop ArtifactViewer 18/18 + Tauri mock E2E; mobile 2286 pass incl. real-WKWebView DOM-click test. Outstanding for the owner: §14 walkthrough on two signed-in accounts/unpaired machines with a real remote, and physical iOS + Android react-native-webview checks — docs/2026-09-23-section-14-two-account-artifact-walkthrough.md. Advanced to panel review.
- 2026-09-23: T2 review round 1 failed at `de8872965`: concurrency — revisit checkout can destroy or hide commits made in the retained directory. Migration pass; 536 baseline tests. Revision 1/5. Follow-ups: pre-098 DB reopen test (NULL session rows, legacy session_ref shape, counter seeding from deleted refs in cwd rows); stale lifecycle.rs comment; merge message carries the Conflicts note.
- 2026-09-23: T6b passed panel review round 2 at `fd6f7e2e9`. Merged into parent; resolved: ledger envelope writes T8c's channel_identity AND T6b's artifacts; complete_stage result enqueue combines T8c provenance with T6b artifact references; preview CSP frame-ancestors = union of T12 desktop-webview origins and T6b shell origins (T6b's shell test assertion updated to the union). Combined check: kanna-server artifact/task_store/actions/input/workflow_switch/mutation_provenance/route-audit/local_config 299 + artifact HTTP 14 pass; two-server artifact_sharing + artifact_tools pass; tool-catalog pass; desktop ArtifactViewer 10/10 + vue-tsc; mobile viewer 21/21 + tsc; core tsc. T6b closed. Follow-ups: sweep-vs-reopen race (reopen can clear closed_at between collectable() and delete/gc — needs lock order + deterministic test); redundant metadata commit on no-op import; second expiry record on re-expiry; persisted fixtures for lagging counter / untimed legacy mark; Unicode folding in is_dot_git; result-name separators / duplicate JSON keys.
- 2026-09-23: T12b review round 1 failed at `517e55f60`: security, UI and concurrency fails, five blocking findings; compat pass. Revision 1/5; also asked to merge parent `1d4eaa650` (T6b) and use T6b's real expiry/binding types.
- 2026-09-23: T1 merged parent `df6ee9040` as `257e08f5a` (266 server + 67 core loader tests pass); advanced to panel review. kanna-mcp stdio_http `serve_infers_create_task_repo_from_current_task_context` and `serve_routes_task_listing_and_creation_to_an_explicit_machine` fail on parent AND at base `9a4198da8` (control; base also fails notify_mobile_surfaces_only_the_fixed_server_rejection_error) → pre-existing, not from this delivery.
- 2026-09-23: T2 review round 2 at `0efe1f573`: round-1 findings still open as a TOCTOU race (commit landing between guard read and switch/delete is lost on rollback or stranded on revisit; reproduced with real git). Revision 2/5. Parent guidance: quiesce Kanna-owned sessions in the directory, CAS ref updates (never delete a moved ref), re-read and report advanced previous branch; tests landing a commit inside each window.
- 2026-09-23: T1 passed panel review round 1 at `257e08f5a` (concurrency, compat pass; migration's downgrade-fence finding judged non-blocking — repo-wide, carried to T13). Merged into parent; resolved with T6b: CompleteStageRequest carries both `artifacts` and `exit`; catalog complete_stage lists both; stage result identity includes both artifacts and exit/workflowDigest; result ledger entry uses enqueue_ledger_entry_with_artifacts with T1's routing fields. Parent fix: `update_pipeline_item_stage_with_trigger` marked #[cfg(test)] (now test-only). Combined check: 652 server tests (exit_routing, stage, revision, task_store, actions, input, workflow_switch, mutation_provenance, artifact, route audit, local_config, transfer) pass; clippy clean except pre-existing nonminimal_bool/large_enum_variant; tool-catalog, MCP artifact_sharing/artifact_tools, core tsc pass.
- Known failures, not from T1: kanna-cli `typed_cli_surfaces_match_catalog_tools_and_params` fails since the T6b merge (T6b added `artifacts` to catalog complete_stage without a typed `stage-complete --artifacts` mapping; T6b's branch has the same gap) → bounded correction child. packages/core agent-loader/qa-assets 3 failures read machine-local agent config; their inputs are unchanged since base → pre-existing.
- T1 follow-ups: downgrade fence (T13); concurrent completion/replacement stress tests; task_stage_budget + ledger not carried by transfer (T9); preamble lists no exits (T10); REREVIEW_VERDICT_COMPLETION_INSTRUCTION mentions request_revision; regenerate pages_build_schema copy.
- 2026-09-23: correction child `72b57433` created for the stage-complete --artifacts CLI mapping.
- 2026-09-23: T12b review round 2 at `50af229ae`: round-1 fixes confirmed; new fails — push not bound between GET and POST, redact() leaks '@/@' password; Cancel during re-resolve doesn't stop push; mobile fetch A→B→A stale overwrite. Revision 2/5 used. Parent guidance: server-side remote fingerprint CAS on push, URL-parser-based redact with hostile-shape table, single abort token across push steps, per-view request tokens.
- 2026-09-23: CLI correction built (`--artifacts` on stage-complete + surface mapping + HTTP test; kanna-cli 121 pass, 1 needs a built server binary); advanced (commit post, then single review).
- 2026-09-23: T2 review round 3 at `cad5c1990`: remaining blocker — revisit does not stop the retained directory's own teardown session before switching. Revision 3/5.
- 2026-09-23: CLI correction passed review at `87f633358`, merged as `4c92ead01`; full kanna-cli suite green on parent (122 + all sub-binaries). Closed.
- 2026-09-23: T2 review round 4 at `9ff7c7baa`: revisit stops td-<HEAD branch> but teardown was named td-<recorded branch>; detached/other-branch directory switched while teardown runs. Revision 4/5 (last). Parent guidance: select sessions to quiesce by directory/workspace_id, not branch; refuse revisit if any cannot be stopped; key teardown by workspace_id; tests for detached / other-branch / recorded-branch with live teardown.
- 2026-09-23: T12b passed panel review round 3 at `48162c567` (security, UI, concurrency, compat all pass: fingerprint-bound push, parser redact, single cancel handle, ordered mobile answers). Merged cleanly; parent fix: T12b's test TaskActionResponse initializer gains T1's `routing: None`. Combined check: server 210 (artifact, route audit, local_config, exit_routing, task_store, actions) pass; MCP artifact_sharing/tools pass; kanna-cli 122; tool-catalog pass; clippy clean (pre-existing lints allowed); desktop full vitest 2362 pass, 3 failing files pre-existing at base (PreferencesPanel.theme, updater, realSuiteNaming) plus localServices flake (3/3 pass on rerun, untouched); desktop vue-tsc; mobile full vitest 2294 pass + tsc. T12b closed.
- OUTSTANDING HUMAN CHECKS (owner): (1) §14 two-account walkthrough, two signed-in accounts on two unpaired machines with a real shared remote; (2) physical iOS and Android react-native-webview checks — both in docs/2026-09-23-section-14-two-account-artifact-walkthrough.md. Until performed, §14/V1 acceptance is unverified.
- 2026-09-23: T2 review round 5 at `02e091e8e`: round-4 findings closed; new blocker caused by the parent's round-4 guidance — td-<directory> teardown naming lets a stale teardown supervisor kill a later teardown in the same directory. Revision 5/5 (budget now spent; a further failure parks T2 for the owner). Parent correction: unique teardown identity per operation recorded against the workspace; supervisors kill only their own session id/pid.
- 2026-09-23: OWNER AUTHORIZATION (in the parent session): up to 5 additional revision rounds for T2 (`cb28da39`). When T2's budget is exhausted and it parks, the parent relays this with kanna_request_revision origin "human" (resets the budget).
- 2026-09-23: T11a `c719f2d9` and T13a `e3991858` created from parent `e69577ba6` (both unblocked by T0; cutover/session parts remain gated).
- 2026-09-23: T2 passed panel review round 6 at `e9d863ab0` (within the original budget; the owner's extra rounds were not needed). Merged into parent; resolved with T1: migrations 097_stage_exit_budget then 098_stage_workspaces (both schemas in test_support; latest-migration assert 098); lifecycle reconcile uses T2's `moved_to()` with T1's `_with_exit` stage updates carrying entry_exit; stage/revision test files keep both sides' appended tests. Parent fix: `task_branch_counter` is test-only → #[cfg(test)] (T2's branch carried the dead-code warning). Combined check: FULL kanna-server bin suite 2218 passed, 1 failed (ksp bounded_request_saturation, pre-existing at base); clippy clean (pre-existing lints allowed); rustfmt clean on resolved files; desktop sessions/composables vitest 662 + vue-tsc; core and mobile tsc. T2 closed. Follow-ups: desktop shell spawn bypasses the server lease (sessions.ts:269, src-tauri daemon.rs:35) so a shell opened in the sub-second revisit window is not quiesced — needs a daemon-side directory fence or lease-backed spawn; supervisor pid sampled at soft deadline; teardown run settlement by exact cwd vs canonicalized sweep; pre-098 DB reopen test.
- 2026-09-23: T3 `f7fc3a78` and T4 `c6db4bb4` created from parent `b70608f54` after T1+T2 review. T11a and T13a told to merge the T2 integration.
- 2026-09-23: T11a build `7e4b16a8b` + merge `89b085820` (TaskLatestRun gains exit/artifacts; desktop TaskHeader and mobile TaskScreen show verdict/message/exit/artifact chips; older servers fall back). Component/store/type checks pass; verdict unverified — no live visual check (desktop TaskHeader row, mobile expanded-title block) → carried as a human glance. Advanced to single review.
- 2026-09-23: T11a review round 1 failed: mobile latest result frozen at first detail read; both clients render an empty result row for an in-flight run. Revision 1/5.
- 2026-09-23: T13a build `1c20d3622` (merge of T2 `710ecb046`): docs/2026-09-23-disk-authority-inventory.md; task_store/rebuild.rs versioned reader + deterministic projector; rebuild_into_new_database (refuses existing target); fixture round trip via real endpoints, rebuilt twice identical, differences asserted exactly equal to NOT_REBUILT (20/20 runs). NOT_REBUILT (input to later T13): repo registration, task agent_type/provider, initial_pipeline, legacy revision_rounds, attention, pin, worktree, stage_workspace, branch_counter, runs without result, run session facts, completion bookkeeping, resolved prompt, revision summary/feedback split, backfilled result provenance, input.run_id without result; run times approximated. Advanced to panel review.
- 2026-09-23: T13a passed panel review round 1 (migration pass). Merged cleanly; combined check: clippy clean, task_store/disk_rebuild/db tests 141 pass. Closed. Inventory follow-ups for the next T13 increment: drop queued_task_input (removed by migration 067), add trusted_peer (013), classify the legacy agent_run table.
- 2026-09-23: T11a passed review round 2 at `e5376f1f8` (mobile stale replay and in-flight empty row fixed). Merged cleanly; combined check: mobile_api 24, desktop TaskHeader+MainPanel 62 + vue-tsc, mobile TaskScreen+state 535 + tsc. Closed. Human glance outstanding: desktop TaskHeader latest-result row, mobile expanded-title block.
- 2026-09-23: T4 build `1d5c9fc8a` (migration 099_task_stage_edges; edges satisfied from upstream T0 ledger; start from recorded SHA; extra bases listed, never merged; later-stage gate; final-stage waits for close; supersession event only; stage-aware cycle check incl. legacy blockers; readiness sweep at startup; legacy blocker API unchanged). 14 new tests; full suite 2229 pass, 4 fail (known ksp + 3 load-sensitive LAN). Gaps: no CLI/MCP catalog field for dependencies; TS prompt-builder has no dependency section; UI still reads task_blocker (T11). Advanced to panel review.
- 2026-09-23: T4 review round 1 failed: five blocking findings (concurrency 3, migration 1, compatibility 1), all verified by the dispatcher. Revision 1/5.
- 2026-09-23: T3 build `e599edb75` (exit_commit via post delivery with dead-session fallback, transition_commit binding, failed commit parks, next stage forks from commit SHA — closes T2's manual-gate limit; roleless Gate stages: fork, setup, park, operator departure with provenance, teardown; schema/loaders lift T1 rejections for these). Tests pass except known ksp + load-sensitive LAN. Gaps: no catalog/CLI for gate summary/artifacts; gate cannot be first stage or loop target; transition_commit not carried by transfer (T9). Advanced to panel review. Migration order fixed: T3 `099_transition_commit`; T4 renumbers to `100_task_stage_edges` and adds the create_task `dependencies` catalog/CLI field.
- 2026-09-23: T3 review round 1 failed: concurrency — a resumed/replaced commit-step run drops its transition_commit binding, so its result bypasses settle-once/no-exit/failure-parks. Revision 1/5.
- 2026-09-23: T4 review round 2 at `7467d2f68`: select+reserve not one transaction; DB that ran branch-only 099_task_stage_edges lacks reserved_* columns (parent ruling: that id never left the child branch — no repair migration); pre-T4 server silently drops `dependencies` (fix: capability advertisement + client refusal). Revision 2/5 with parent guidance.
- 2026-09-23: T3 passed panel review round 2 at `2f91f9ad2` (restarted commit step keeps its operation; concurrency pass). Merged cleanly. Combined check: FULL kanna-server suite 2252 passed, 1 failed (ksp pre-existing); clippy clean; core workflow-loader 77/77 + tsc. T3 closed. Follow-ups: repo-wide downgrade/version gate for lifecycle intents and stored workflow fields (T13); fault-injection tests for provider fallback/rerun supersession; semantic schema-parity fixtures; gate summary/artifacts in catalog/CLI; commitStep in desktop/mobile TS types (T11); transition_commit not carried by transfer (T9); gates not first stage / loop target / rerun.
- 2026-09-23: T14 `6b069969` created from parent `c05999e0f` after T3 review. T4 told to merge parent (T3's 099) before its next review.
- 2026-09-23: T4 passed panel review round 3 at `19f785fdb` (one-transaction select+reserve; stageDependenciesVersion capability with CLI/MCP refusal on older servers; migration 100 after T3's 099; roleless gate compatibility). Merged cleanly. Combined check: FULL kanna-server suite 2277 passed, 1 failed (ksp pre-existing); clippy clean for server/cli/mcp; kanna-cli 125; tool-catalog; MCP artifact tests. T4 closed. Follow-ups: explicit synchronization instead of 300 ms sleep in the race test; 099→100 upgrade fixture with T3 rows; permanent adapter regression tests for remote-old/local-new refusal.
- 2026-09-23: T5 `8769b94e` and T11b `43be5d42` created from parent `3b11ea341` after T4 review.
- 2026-09-23: T14 build `0145b8de3` (base identity fixture) + `16c0065db`: release.json (merge window → qa gauntlet gate → ship staging → soak gate → ship production, all manual, no timer/publish); singleton claim pins release under unchanged "singleton-merge" identity (byte-identical fixture); quiescent idempotent startup migration of live masters; handoffs outside the merge window refused 409; policy.handoff "merge" on a final stage replays the approve-post handoff. Full suite 2257 pass, 4 environment/known failures pass on rerun. Advanced to panel review.
- OWNER PRODUCT QUESTION (from T14): once a merge master is advanced past its merge window into QA/soak, new PR handoffs are refused until that release closes (today the singleton accepts handoffs at any time; behaviour only changes if someone advances the master). Should a new merge window (new singleton claim) open while an earlier release is still in QA/soak?
- 2026-09-23: T14 review round 1: release, security, migration, compatibility pass; concurrency fail. Revision 1/5.
- 2026-09-23: T14 review round 2 at `4d2feac1e`: concurrency — daemon idle cache not invalidated by input, so a turn can start after the quiescence check. Revision 2/5. Parent guidance: drop the idle probe; migration only needs exclusion of workflow/lifecycle mutations under the task mutation lease (unchanged current-stage binding supersedes no run).
- 2026-09-23: T5 build `f73b8e057` (migration 101_subtask_joins: task_join + task_join_member; exactly-once resolution in the resolving transaction with parent input + ledger entry; parent gating via blocking ids; close guard; POST /subtasks + GET /joins; startup sweep; subtaskJoinsVersion capability; kanna_create_subtasks/kanna_get_task_joins with client refusal). Tests pass except known ksp + pre-existing stdio_http. Gaps: mobile blocked-with-live-session → sent to T11b; join tables not rebuilt from disk (T13); transfer with open join (T9); no idempotency key on POST /subtasks. Advanced to panel review.
- 2026-09-23: T14 passed panel review round 3 at `c9a782a9b` (idle probe dropped; migration fenced on workflow/lifecycle mutations under the task mutation lease; release/security/migration/compat pass). Merged; runtime.rs startup keeps both T4's dependency-readiness sweep and T14's merge-singleton migration. Combined check: full suite 2282 pass, 7 fail — ksp (pre-existing) + 6 LAN/cloud invoke tests that pass 31/31 twice in isolation (load 14–16); clippy clean; core loader 82/82 + tsc; kanna-cli 125. T14 closed. Follow-ups: full handler contention test for migration; custom/inline definitions can still bind the merge agent outside the claim (pre-existing); handoff:null Rust vs TS parity fixture. Owner product question on concurrent merge windows remains open.
- 2026-09-23: T5 review round 1: concurrency — close guard misses recorded-but-uncreated members; T4 readiness completion path bypasses the join gate; startup sweep can re-create a created member. Mobile live-parent detach judged non-blocking (T11b). Migration pass. Revision 1/5; asked to merge parent `7acc9197f` (T14).
- 2026-09-23: T11b build (uncommitted; server TaskDetail sessionHistory/stageDependencies/dependencyWait/gateParked; desktop TaskHeader + mobile TaskScreen session/gate/dependency display; mobile live-session vs blocked distinction). Parent sent back before review: wire workspace_id from the server task projection so identity isn't branch-derived in practice; ja/ko strings; merge parent (T14).
- 2026-09-23: T5 review round 2 at `40e37d957`: round-1 findings closed; two new concurrency defects in the round-2 join gate and release path. Revision 2/5.
- 2026-09-23: T5 passed panel review round 3 at `c740589e2`. Merged cleanly. Combined check: FULL kanna-server suite 2305 passed, 1 failed (ksp pre-existing); clippy clean; kanna-cli 126; tool-catalog. T5 closed. Follow-ups: overlapping notice/startup/publisher release tests; owed-continuation variant through startup recovery with T4/T14 overlap; SubtaskSpec deny_unknown_fields needs format versioning; T13 must restore join tables from disk.
- 2026-09-23: T9 `6cc08860` and T10 `0e2062af` created from parent `42ba8ed70` after T5 review. `designed` must not become default while the §14 human walkthrough is outstanding.
- 2026-09-23: T10 asked how to scope the full card; parent narrowed it to the first increment. Remaining lineup conversion (other agents/flavors/workflows, qa-dispatcher via joins, merge/release policy layer) will be follow-on children.
- 2026-09-23: T10 first increment `d84a61b1e` (opt-in formula frontmatter role/providers with resolved-definition checks in Rust + core; implement and pr converted to 17-line four-section definitions; new bundled `mechanical` workflow implement→pr with exits, exit_commit, policy.handoff merge; end-to-end fixture from ledger/preamble). 1306 server + 334 core tests pass. Review must check pr's compressed git recipe preserves policy. Advanced to panel review after the syspolicyd stall cleared (children were on standby ~20 min).
- Routing: review stage of new children switched claude-fable → claude-opus-med (parent-owned per-task edit). Proposed config.local.json change (specialty reviewers: security/concurrency pinned to gpt-6-astra low, others gpt-6-sol medium) awaits owner; commit agent stays as is — commit runs in the implementer's live session under the spec (owner).
- Disk free 43 GiB (down from 90 GiB); monitor.
- 2026-09-23: T10 review round 1 failed: converted pr definition drops the obligation to record success with the PR URL, so task.prUrl stays unset and mechanical's handoff backstop refuses close. Revision 1/5.
- 2026-09-23: Disk: owner asked to clear all caches/build dirs in the worktrees. `kd clean --all` run in every worktree except T10's (active build) — free space 43 GiB → 150 GiB. T9 and T11b told to re-run setup.
- 2026-09-23: T9 build `6b9ccdeb0` (task_state_version capability, refusal while anything is owed, task-state.json + history/artifact bundles bound by sha256, ledger re-recorded under the new id, verified before ack, fresh session from ledger; migration 102_transferred_task_state). Targeted suites pass; its full-suite run overlapped the syspolicyd stall (43 failures, all pass by name) — parent reruns at integration. Limits: other stages' workspaces/edges/joins arrive as records, not FK rows; claim-time refusal surfaces as 500. Advanced to panel review.
- 2026-09-23: T10 passed review round 2 at `f4f1e855b` (pr records success with the PR URL). Merged cleanly; combined check after kd setup: server task_creator/definitions/agent 518 pass, clippy clean, core workflow vitest 247 pass (3 pre-existing agent-loader/qa-assets failures), core tsc. T10 closed.
