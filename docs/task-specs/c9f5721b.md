# LAN authorization boundary

Close security finding #1 from owner review f1c3ca89 (2026-09-05): unpaired LAN callers must not access task, repository, settings, or file HTTP data. Audit all listener routes and enforce pairing by default at the router, preserving direct loopback and authenticated relay behavior and the existing 401 text refusal. Pairing/discovery bootstrap stays available; KSP's in-band authentication must remain compatible.

Scope: kanna-server Rust, authorization tests, boundary documentation, and mobile only if pre-pairing calls require correction. No local credential/CORS redesign, unrelated review findings, protocol bump, or staging mutations. Read the owner's review worktree only.

Done: exhaustive route audit and router regression coverage, paired/unpaired route tests, mobile pairing-flow verification, real second-host LAN probes or a dated E2E gap note with route tests, cargo fmt/clippy and ./kd test all. The table below enumerates all 111 registered path patterns, including debug-only routes.

## Listener route audit

All routes are registered in `http_api/router.rs`; `mobile_api.rs` is a domain/data module, not another listener. GET includes implicit HEAD. Default authorization also covers OPTIONS and unsupported methods. Loopback-only entries retain stricter handler checks after the common guard.

| Route | Methods | Classification |
|---|---|---|
| `/v1/status` | GET | intentionally unauthenticated discovery/health |
| `/v1/snapshot` | GET | LAN-paired-required |
| `/v1/backup` | POST | LAN-paired-required |
| `/v1/cloud/relay/actions/reconnect` | POST | LAN-paired-required |
| `/v1/cloud/desktops` | GET | loopback-only |
| `/v1/cloud/desktops/{desktop_id}/invoke` | POST | loopback-only |
| `/v1/settings/cloud-transfer-identity` | PUT | loopback-only |
| `/v1/settings/{key}` | GET, PUT, DELETE | LAN-paired-required |
| `/v1/window-workspace/mutations` | POST | LAN-paired-required |
| `/v1/operator-events` | POST | LAN-paired-required |
| `/v1/mobile/notifications` | POST | LAN-paired-required |
| `/v1/mobile/notifications/registration` | GET | LAN-paired-required |
| `/v1/analytics/repos/{repo_id}` | GET | LAN-paired-required |
| `/v1/stream` | GET | LAN-paired-required; upgrade is authentication bootstrap; pairing verified before task data |
| `/v2/stream` | GET | LAN-paired-required; upgrade is authentication bootstrap; pairing verified before task data |
| `/v1/desktops` | GET | LAN-paired-required |
| `/v1/repos` | GET, POST | LAN-paired-required |
| `/v1/repo-checkouts` | POST | LAN-paired-required |
| `/v1/repo-checkouts/{operation_id}` | GET | LAN-paired-required |
| `/v1/repos/by-path` | GET | LAN-paired-required |
| `/v1/repos/actions/reorder` | POST | LAN-paired-required |
| `/v1/repos/{repo_id}` | PATCH | LAN-paired-required |
| `/v1/repos/{repo_id}/reconcile-metadata` | POST | LAN-paired-required |
| `/v1/repos/{repo_id}/tasks` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/agents` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/commands` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/commands/{command_id}/run` | POST | LAN-paired-required |
| `/v1/repos/{repo_id}/kanna-definitions` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/fetch-origin` | POST | LAN-paired-required |
| `/v1/repos/{repo_id}/kanna-definitions/workflows/{workflow_name}` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/kanna-definitions/pipelines/{workflow_name}` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/kanna-definitions/agents/{agent_selector}` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/recent-workflows` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/recent-pipelines` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/agent-providers` | GET | LAN-paired-required |
| `/v1/repos/{repo_id}/agents/{agent}/signal` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/release-closed-singleton-reservation` | POST | LAN-paired-required |
| `/v1/repo-singletons/{remote_url_hash}/{agent}` | GET | LAN-paired-required |
| `/v1/task-events` | GET | LAN-paired-required |
| `/v1/tasks/recent` | GET | LAN-paired-required |
| `/v1/tasks/search` | GET | LAN-paired-required |
| `/v1/tasks/closed-identities` | GET | LAN-paired-required |
| `/v1/tasks` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}` | GET, PUT, PATCH | LAN-paired-required |
| `/v1/tasks/{task_id}/children` | GET | LAN-paired-required |
| `/v1/tasks/{task_id}/inputs` | GET | LAN-paired-required |
| `/v1/tasks/{task_id}/files/content` | GET | LAN-paired-required |
| `/v1/tasks/{task_id}/browse` | GET | LAN-paired-required |
| `/v1/tasks/{task_id}/browse/content` | GET | LAN-paired-required |
| `/v1/tasks/{task_id}/files/resolve-mentions` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/diff` | GET | LAN-paired-required |
| `/v1/tasks/{task_id}/dependent-tasks-exist` | GET | LAN-paired-required |
| `/v1/tasks/{task_id}/logs` | GET | LAN-paired-required |
| `/v1/tasks/{task_id}/input` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/raw-input` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/block` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/unblock` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/runtime-status` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/mark-read` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/agent-session` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/agent-session-id` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/advance-stage` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/rerun-stage` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/resume` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/complete-stage` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/signal-merge-handoff` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/request-revision` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/set-parent` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/set-workflow` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/set-pipeline` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/pin` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/unpin` | POST | LAN-paired-required |
| `/v1/tasks/actions/reorder-pinned` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/close` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/abort-creation` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/reopen` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/ports` | POST, DELETE | LAN-paired-required |
| `/v1/tasks/{task_id}/preview` | POST, DELETE | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/run-merge-agent` | POST | LAN-paired-required |
| `/v1/tasks/{task_id}/actions/cloud-task-identity` | PUT | LAN-paired-required |
| `/v1/transfers/incoming/pending` | GET | LAN-paired-required |
| `/v1/transfers/incoming/cleanup-candidates` | GET | LAN-paired-required |
| `/v1/transfers/outgoing/active/{source_task_id}` | GET | LAN-paired-required |
| `/v1/transfers` | POST | LAN-paired-required |
| `/v1/tasks/{source_task_id}/actions/push-to-peer` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/approve` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/reject-incoming` | POST | LAN-paired-required |
| `/v1/transfers/provenance` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}` | GET | LAN-paired-required |
| `/v1/transfers/{transfer_id}/payload` | PUT | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/complete` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/importing` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/awaiting-acknowledgment` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/reject` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/claim` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/renew-claim` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/fail` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/fail-outgoing` | POST | LAN-paired-required |
| `/v1/transfers/{transfer_id}/actions/sidecar-cleanup-complete` | POST | LAN-paired-required |
| `/v1/transfers/sidecar/events` | GET | loopback-only |
| `/v1/transfers/sidecar/companion-events` | GET | loopback-only |
| `/v1/transfers/sidecar/control/{operation}` | POST | loopback-only |
| `/v1/transfers/cloud-proxies` | POST, DELETE | loopback-only |
| `/v1/transfers/cloud-proxies/{peer_id}` | DELETE | loopback-only |
| `/v1/pairing/sessions` | POST | loopback-only |
| `/v1/pairing/sessions/claim` | POST | intentionally unauthenticated pairing bootstrap (code required) |
| `/v1/pairing/push-certificate` | POST | LAN-paired-required |
| `/v1/pairing/trusted-devices/{device_id}` | DELETE | loopback-only |
| `/v1/e2e/sql` | POST | loopback-only |
| `/v1/e2e/server-work` | POST | loopback-only |
| `/v1/e2e/mobile-machine-controls` | POST | loopback-only |

Task-file content/mention routes retain their existing additional relay/file authority restrictions. Preview proxy listeners (`/`, `/{*path}` on separately allocated ports) are created only by an authorized preview request and require their short-lived entry secret/cookie before proxying; they are not mounted on the LAN API router. The debug SQL/server-work endpoints additionally retain their existing authenticated in-process test adapter (synthetic loopback) when the E2E environment gate is enabled. No standalone `/health` route exists; `/v1/status` is discovery/health.

## Mobile and compatibility

Reviewed `machinePairing.ts` (claim before storing the issued secret),
`appModel.ts` (trusted endpoint resolution and credential-bound clients),
`trustedBonjour.ts` (status-only discovery), `lanTransport.ts` (device headers
on HTTP and credential in KSP Auth), and `cloudLanClient.ts` (requires running
status before repository/task reads). Account-known but unpaired endpoints
report `pairing_required` and retain cloud fallback; no mobile change is needed.
Old trust records without secrets need re-pairing to use LAN data.

The audit found legacy `/v1/stream` empty-auth reads would bypass the requested
no-data boundary. Both LAN upgrade handlers now require pairing before task data. Unverified
upgrades select existing strict in-band paired KSP authentication. Legacy v1
readers already verified via headers/cookie keep their existing read-only
empty-Auth behavior; direct loopback stays empty-auth compatible. No frame,
paired-client refusal, or version changes. The audit also found the KSP
state-change forwarder subscribed at connection startup, before Auth. It now
subscribes around the authentication handshake and starts forwarding only
after success, so an idle unauthenticated upgrade cannot receive task-state
notifications. A paused-clock regression test proves pre-auth silence and
post-auth delivery. Local browser/CORS trust redesign
from the broader review is deliberately outside this assignment.

Second-host verification gap: `docs/2026-09-05-lan-auth-e2e-gap.md`.

## Verification coverage

The route-enumeration test reads the actual router registrations, requires each
path in this audit, and sends every HTTP method with missing and invalid LAN
credentials through the production router. It includes HEAD, OPTIONS, legacy
aliases, debug routes, and methods unsupported by handlers. Adding nested or
service mounts requires extending the enumeration rather than silently skipping
them. Refusals assert the exact existing 401 body (HEAD has no body).

Two paired route matrices exercise the newly guarded settings, snapshot, backup,
workspace, event, task-read, analytics, repository, definitions, command, and
checkout surfaces with valid fixture data. The settings/data matrix also proves
direct loopback and authenticated invokes still succeed while unauthenticated
invokes fail. Repository tests use local git fixtures and wait for checkout to
finish. A real ephemeral listener verifies unpaired settings/repo refusal,
loopback-created/LAN-claimed pairing, successful paired reads, and missing,
invalid, and valid KSP credentials on both v1 and v2 upgrades, plus the
legacy paired header/cookie read-only compatibility path. Existing task,
file, pairing/bootstrap, and status tests cover the retained stricter contracts.

## Final results (2026-09-05)

- `./kd test all` passed: workspace JavaScript/mobile suites, generated protocol
  checks, desktop build, sidecar builds, Rust workspace and serialized daemon
  suites. The final server unit run passed all 1,251 tests; the focused HTTP
  run passed 351 tests. An earlier broad run timed out in the unchanged daemon
  reconnect test `same_id_reuse_waits_for_old_reader_exit_and_recovery_teardown`;
  it passed in the final complete run. No daemon code was changed.
- `cargo fmt --all` and `git diff --check` passed.
- `cargo clippy -p kanna-server --all-targets` exited successfully. It is not
  warning-free: pre-existing dead-code, argument-count, lock-across-await,
  and other warnings remain outside the changed code (15 production warnings,
  41 test warnings including duplicates). No warnings were introduced or
  suppressed; unrelated warning cleanup remains with 4068f128.
- Mobile pairing/discovery, credential transport, and cloud fallback were
  inspected and their existing suites passed in the canonical run. No mobile
  implementation or native/runtime version change was necessary.
- Real network probes used only freshly created ephemeral test listeners and
  fixture databases/pairing stores. Staging was not probed or modified. Tests
  shut down their listeners; no persistent dev service was started. A second
  physical host was not exercised; the dated E2E gap note records that limit.
