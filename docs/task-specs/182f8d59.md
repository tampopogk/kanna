# Restore the mobile relay E2E harness

## Goal

Fix the two-instance mobile relay E2E harness so its deterministic fixture proves the current large-history/windowing contract, finds the real published cloud task row, and reaches and passes the task journeys, including task 33213414's terminal-keys journey.

## Scope and constraints

- Trace and document both harness failures, then change only E2E harness/fixture code and documentation.
- Do not alter product behavior to accommodate a fixture; report any product regression separately.
- Owner directive, 2026-09-05: after task `392e1e6e` independently fixed and verified the unrelated native composer regression, cherry-pick its final amended product fix `865e8d2ddf9973f0188f94e5d0621aa0846014e1` and run this branch's authoritative full relay lane. The owner explicitly superseded two earlier hashes. This permits integrating that reviewed fix; the relay assertion remains strict and no fixture accommodation is allowed.
- Integrate the terminal-keys E2E test from task 33213414 if it has not merged.
- Keep touched TypeScript packages clean under `pnpm exec tsc --noEmit`.
- Correct or narrow `docs/2026-09-05-mobile-terminal-keys-e2e-gap.md` once the full journey runs.
- Stop every simulator/dev/background process started during verification by its recorded identity.

## Done

`KANNA_IOS_SIMULATOR_NAME='iPhone 17 Pro' ./kd test remote-e2e --dev --mobile-relay` reaches and passes the existing task journeys and terminal-keys journey on this machine, with both root causes and fixes recorded here.

## Root causes and fixes

1. PR #1273's windowed KSP terminal protocol intentionally replaced the full initial VT snapshot with a bounded recent window and separately advertised retained scrollback. The scripted fixture still generated 10,050 deterministic history lines, but its precondition measured only the now-bounded `dataB64` field and therefore waited for an impossible 1,000,001 encoded characters. The harness now proves the current contract instead: the initial window is bounded below 100,000 encoded characters, contains the sentinel, and advertises at least 9,000 retained scrollback lines. A live focused run observed 7,356 encoded characters, 5,517 decoded bytes, and 9,987 retained lines. Relaxing the old size assertion is correct because a negotiated mobile client no longer exercises an oversized initial-snapshot path; retaining it through a legacy/unwindowed observer would test a different protocol from the app.
2. The portable cloud ownership work publishes a stable `cloudTaskId` for real server tasks, and mobile deliberately uses that identity ahead of the legacy `cloud:<desktop>:<repo>:<local-task>` fallback. The relay successfully wrote the Firestore task document, but the Appium harness kept looking for the fallback selector and reported the existing row as absent. The publication preflight now returns the row's authoritative `cloudTaskId` (falling back only for legacy documents), and all later task journeys use that id.

## Additional harness drift exposed after setup

- Current task cards include `Task ID <id>` in their accessibility label. The relay assertions now match that intentional label.
- The deterministic ordering rows can put the real task outside the iPhone 17 Pro viewport. Known-row assertions and opens now scroll the exact row into view instead of equating `displayed` with `published`.
- The former Recent tab is now unread-only Activity. The journey explicitly drives the fixture to `unread` for its Activity-row assertion and restores `working` before the existing activity-transition checks.
- The snapshot observer previously accepted the first matching-content attach snapshot and only then compared geometry. Snapshot expectations can now include rows and columns so a geometry assertion waits for the requested state.
- The relay fixture now creates its PTY at the deterministic, authoritative mobile geometry (80x48). A separate product-wiring regression was exposed while doing so: mounting the real mobile detail did not resize an initially 80x24 PTY. No product code was changed here; follow-up task `2462ddd0` owns that investigation.
- The long retained-history fixture's independent terminal observer is a rendered-window observer, not a durable receipt ledger. Its `outputText()` can be replaced by KSP recovery snapshots, and live PTY responses are not guaranteed to remain observable there while the heartbeat advances the bounded window. This caused false negatives first for terminal-key markers and then for submitted-input markers even though server/daemon tracing proved the bytes reached the child. The scripted fixture now appends exact child-read key acknowledgements and NUL-delimited multiline inputs to files in its disposable worktree. Terminal keys, quick reply, and file-preview setup assert those deterministic ledgers; quick reply also asserts exactly one matching durable server `task_input` row.

## Terminal-keys integration

Task `33213414` committed its feature and Appium journey as `eb5a19710`; this branch cherry-picked it as `b8a882ece`. The repaired relay suite reaches and passes `verifyRelayTerminalKeys`. Temporary tracing performed by that task proved the exact Esc byte traversed TaskScreen, controller, relay client, StreamClient, server KSP decode, terminal-control worker, daemon command receipt, pending-input writer, and a completed one-byte write to the correct child PTY. No product correction was required; the false negative was the rendered-window observation described above, and the strict child-receipt assertion remains.

## Independently fixed product blocker

The exact suite reached every journey through terminal keys, quick reply, task actions, file preview, and visual companion, then exposed an unrelated rendered-product regression: after Send, the value cleared and the iOS keyboard dismissed, but the native input stayed at its expanded 80 pt height instead of returning to the 40 pt one-line baseline. Separate task `392e1e6e` fixed this by pinning the existing native `TextInput` to 40 pt only while its controlled draft is empty and removing that height during edits so intrinsic multiline measurement still works. Per the owner directive above, this branch integrated final source commit `865e8d2ddf9973f0188f94e5d0621aa0846014e1`; the unchanged Appium expectation is the authoritative integration check and passes.

## Verification record

- `pnpm --dir tests/remote-e2e exec tsc --noEmit`
- `pnpm --dir apps/mobile exec tsc --noEmit`
- `pnpm --dir packages/stream-client exec tsc --noEmit`
- `pnpm --dir apps/mobile exec vitest run e2e/helpers/relay-harness.test.ts e2e/specs/relay/relay-task-flow.test.ts src/screens/taskTerminalKeys.test.ts` (46 passed)
- `pnpm --dir tests/remote-e2e exec vitest run src/scriptedAgent.test.ts --maxWorkers=1` (11 passed)
- Post-integration mobile focus (`TaskScreen`, relay harness/journey, terminal keys): 136 tests passed; the full `tests/remote-e2e` package suite passed 26 tests.
- Focused live `startMobileRelayHarness()` run reached ready state and confirmed both the bounded-window/history precondition and cloud publication.
- `KANNA_IOS_SIMULATOR_NAME='iPhone 17 Pro' ./kd test remote-e2e --dev --mobile-relay` passed from a cold iOS 26.5 simulator/WDA state. It completed relay setup, cloud publication, task-list navigation, activity transitions, mark-read, snapshot revisit, terminal keys, quick reply, task actions, file preview, visual companion, and the strict composer-reset journey.
