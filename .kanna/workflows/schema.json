{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.kanna.build/workflow.schema.json",
  "title": "Kanna Workflow Definition",
  "description": "Schema for .kanna/workflows/*.json workflow definitions.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "stages"],
  "$defs": {
    "agentProvider": {
      "enum": ["claude", "copilot", "codex", "opencode", "antigravity"]
    },
    "agentProviderSpec": {
      "description": "A compact provider selector: provider[-model[-effort]]. A plain provider id (see agentProvider) inherits the CLI's own model and effort; an optional model rides after the provider (passed to the CLI verbatim, so multi-segment ids like gpt-5.6-sol work); a trailing effort token (lo/low, med/medium, hi/high, xhi/xhigh, max) sets the reasoning effort. Examples: claude, codex-gpt-5.6-sol, claude-fable-hi, codex-gpt-6-astra-lo. A model is rejected for providers without a model flag, and effort is checked against the provider's published vocabulary when one exists.",
      "type": "string",
      "pattern": "^(claude|copilot|codex|opencode|antigravity)(-[A-Za-z0-9][A-Za-z0-9._-]*)?$"
    },
    "agentProviderSelection": {
      "oneOf": [
        {
          "$ref": "#/$defs/agentProviderSpec"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/agentProviderSpec"
          },
          "minItems": 1
        }
      ]
    }
  },
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string"
    },
    "visibility": {
      "description": "Whether this workflow is offered as a choice. \"internal\" keeps the name out of every listing (the repo manifest, the desktop's new-task picker, the tool catalog's advertised lineup) while it still resolves when named explicitly on task creation — visibility governs listing, not access. Defaults to \"public\" when absent.",
      "enum": ["public", "internal"]
    },
    "revision_limit": {
      "description": "Agent-requested revision rounds a task may spend before Kanna stops forking revisions and parks the task for its human. Defaults to 5 when omitted; 0 disables the cap. A human-requested revision is never refused and resets the count.",
      "type": "integer",
      "minimum": 0
    },
    "environments": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "setup": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "teardown": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "stages": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "policy"],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "description": {
            "type": "string"
          },
          "agent": {
            "type": "string"
          },
          "prompt": {
            "type": "string"
          },
          "agent_provider": {
            "$ref": "#/$defs/agentProviderSelection"
          },
          "environment": {
            "type": "string"
          },
          "policy": {
            "type": "object",
            "additionalProperties": false,
            "required": ["transition"],
            "properties": {
              "transition": {
                "enum": ["manual", "auto"]
              },
              "revision_transition": {
                "description": "Completion policy for main runs entered through a revision request. Defaults to transition when omitted.",
                "enum": ["manual", "auto"]
              }
            }
          },
          "post": {
            "type": "object",
            "description": "Tail work injected into the stage's running agent session when the stage transitions forward. `agent` is the fallback spawned when the session is dead.",
            "additionalProperties": false,
            "required": ["name", "prompt"],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1
              },
              "description": {
                "type": "string"
              },
              "agent": {
                "type": "string"
              },
              "prompt": {
                "type": "string"
              },
              "agent_provider": {
                "$ref": "#/$defs/agentProviderSelection"
              }
            }
          }
        }
      }
    }
  },
  "examples": [
    {
      "$schema": "./schema.json",
      "name": "default",
      "description": "Standard in progress (commit as post) -> PR flow",
      "stages": [
        {
          "name": "in progress",
          "agent": "implement",
          "prompt": "$TASK_PROMPT",
          "policy": { "transition": "manual", "revision_transition": "auto" },
          "post": {
            "name": "commit",
            "agent": "commit",
            "prompt": "Commit the relevant work for this task. Original task: $TASK_PROMPT"
          }
        },
        {
          "name": "pr",
          "agent": "pr",
          "prompt": "Create a PR for the work on branch $BRANCH. Previous result: $PREV_RESULT",
          "policy": { "transition": "manual" }
        }
      ]
    },
    {
      "$schema": "./schema.json",
      "name": "orchestrated-child",
      "description": "A workflow an orchestrating agent binds for the child tasks it creates; internal keeps it out of the new-task picker while it still resolves by name",
      "visibility": "internal",
      "stages": [
        {
          "name": "review",
          "prompt": "$TASK_PROMPT",
          "policy": { "transition": "manual" }
        }
      ]
    }
  ]
}
