{
  "$schema": "./schema.json",
  "name": "research",
  "description": "Product research: explore what outcome to pursue and why, then pause for owner discussion without implementation. When the owner chooses an outcome and asks to proceed, the task manager appends a manual plan stage to this same task rather than starting a replacement one.",
  "stages": [
    {
      "name": "research",
      "description": "Researcher evaluates alternatives, evidence, tradeoffs, assumptions, and unknowns, records a recommendation, and remains at the manual owner discussion gate; delivery stages are appended to this task only on the owner's explicit instruction",
      "agent": "researcher",
      "prompt": "$TASK_PROMPT",
      "policy": { "transition": "manual" }
    }
  ]
}
