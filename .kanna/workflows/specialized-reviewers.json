{
  "$schema": "./schema.json",
  "name": "specialized-reviewers",
  "description": "Use when a single change genuinely spans several specialty domains at once — security, migrations, concurrency, release — each needing its own dedicated reviewer rather than one generalist's best effort. Implement (commit as post) -> dispatched specialty review panel -> PR. Line count is not the trigger: most work, even large work, is better served by single-reviewer's one reviewer; reach for this only when the domains actually named are plural.",
  "revision_limit": 5,
  "stages": [
    {
      "name": "in progress",
      "description": "Agent implements the task, then commits as the stage's tail work",
      "agent": "implement",
      "prompt": "$TASK_PROMPT",
      "policy": { "transition": "manual", "revision_transition": "auto" },
      "post": {
        "name": "commit",
        "agent": "commit",
        "prompt": "Commit the relevant work for this task before review. Original task: $TASK_PROMPT. Previous implementation result: $PREV_MAIN_RESULT"
      }
    },
    {
      "name": "review",
      "description": "Dispatcher selects the specialty reviews this branch needs, fans them out as child tasks, and aggregates their verdicts",
      "agent": "qa-dispatcher",
      "prompt": "Dispatch the specialty reviews needed for branch $BRANCH against base $BASE_REF, then aggregate their verdicts into one review decision. On a later round, review what changed since the previous review round rather than the whole branch again. Original task: $TASK_PROMPT. Previous implementation result: $PREV_MAIN_RESULT",
      "policy": { "transition": "auto" }
    },
    {
      "name": "pr",
      "description": "Agent creates a GitHub PR after the specialty panel passes",
      "agent": "pr",
      "prompt": "Create a PR for the reviewed work on branch $BRANCH.",
      "policy": { "transition": "manual" },
      "post": {
        "name": "approve",
        "agent": "approve",
        "prompt": "Approve the PR for branch $BRANCH after PR creation and signal the merge master. Previous result: $PREV_RESULT"
      }
    }
  ]
}
