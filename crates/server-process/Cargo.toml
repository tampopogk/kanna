[package]
name = "kanna-server-process"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
tokio = { version = "1", features = ["macros", "process", "rt", "time"] }

[dev-dependencies]
tokio = { version = "1", features = ["io-util", "macros", "process", "rt", "time"] }
