[package]
name = "kanna-tool-catalog"
version = "0.1.0"
edition = "2021"

[dependencies]
kanna-runtime-defaults = { path = "../runtime-defaults" }
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"
