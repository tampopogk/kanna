name: Linux Release Check

# The Linux release build, its dependency audit, and — under the 2026-09-09
# owner directive — the x86-64 installed-acceptance host.
#
# The directive: the Intel Mac that was to run the native x86-64 Ubuntu 24.04
# acceptance VM is in Japan and unavailable for at least a month, and nothing
# may block on it. The substitute chosen is the `ubuntu-24.04` hosted runner
# itself — genuinely the supported floor's userspace. Whether it also carries a
# *working* systemd user manager was asserted here, not measured; the
# `installed-check-prerequisites` job below measures it directly instead of
# trusting the installed harness's own unreliable check. What the runner cannot
# supply either way: a phone-reachable LAN, a human at a screen, and a clean
# machine (a hosted runner is the opposite of one: it ships with Rust, Node and
# compilers preinstalled), and its kernel is not 6.8. All of that is recorded
# in the support matrix of
# `docs/2026-09-09-linux-phase3-supported-distribution.md`.
#
# This workflow builds the declared Bazel packages and runs install-only checks.
# Two-version upgrade acceptance still requires a real predecessor package from
# another revision; no synthetic second version is manufactured here.
# Run the existing upgrade lane with a real pair when it becomes available:
# `./kd test linux-installed --old-artifact <deb> --new-artifact <deb>`.
#
# This workflow is deliberately independent of Pages deployment and holds no
# signing or publishing credential. A Linux build failure must never be able to
# block a macOS ship or a schema publish.

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      prepared_pair_asset_id:
        description: GitHub draft validation asset ID for the exact stamped A/B tar (no build)
        type: string
        required: false
      prepared_pair_sha256:
        description: SHA256 of that tar; required with prepared_pair_asset_id
        type: string
        required: false
      apt_interop_only:
        description: Run only test-key apt/GnuPG interoperability (no native builds)
        type: boolean
        default: false
  pull_request:
    paths:
      - "BUILD.bazel"
      - "MODULE.bazel*"
      - "tools/bazel/**"
      - "tools/kd/BUILD.bazel"
      - "tools/kd/tests/linux-product-audit.ts"
      - "packaging/linux/**"
      - "tests/linux-installed/**"
      - "crates/**"
      - "apps/desktop/src-tauri/**"
      - "tools/kd/src/runtime/linux-*.ts"
      - "tools/kd/src/runtime/release-platform.ts"
      - ".github/workflows/linux-release-check.yml"
  push:
    branches:
      - main
    paths:
      - "BUILD.bazel"
      - "MODULE.bazel*"
      - "tools/bazel/**"
      - "tools/kd/BUILD.bazel"
      - "tools/kd/tests/linux-product-audit.ts"
      - "packaging/linux/**"
      - "tests/linux-installed/**"
      - ".github/workflows/linux-release-check.yml"

# Read-only. This lane builds and tests; it does not publish, and it must not be
# able to.
permissions:
  contents: read

concurrency:
  # Its own group, so a Linux run never queues behind or cancels the Pages
  # deployment.
  group: linux-release-check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apt-interop:
    if: ${{ !inputs.prepared_pair_asset_id }}
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-24.04, ubuntu-24.04-arm]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm --filter @kanna/kd install --frozen-lockfile --ignore-scripts
      - name: Build and validate the actual Node adapter bundle
        run: |
          pnpm --dir tools/kd exec tsup --out-dir ../../.tmp/apt-adapter-bundle
          node tools/kd/tests/apt-bundle-smoke.mjs .tmp/apt-adapter-bundle
      - name: Test disposable signatures with Ubuntu apt and GnuPG
        # Uses existing runner tools, loopback HTTP and private apt state.
        # No sudo, package installs, stored private keys or publication.
        run: pnpm --dir tools/kd exec tsx tests/linux-apt-interop.ts

  build:
    if: ${{ !inputs.apt_interop_only && !inputs.prepared_pair_asset_id }}
    # Native parity for the same Bazel graph used by the macOS cross builder.
    strategy:
      fail-fast: false
      matrix:
        include:
          - architecture: x86_64
            runner: ubuntu-24.04
          - architecture: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      # A hosted runner advertises limited storage and a full Rust release
      # build of this workspace is large. Reclaiming the preinstalled toolchains
      # this lane does not use is disposable runner data, never anything the
      # build produces.
      - name: Reclaim runner disk
        run: |
          df -h /
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc "$AGENT_TOOLSDIRECTORY" || true
          df -h /

      # Bazel owns Rust, Zig, Node and the target sysroot. No host GTK/C++
      # development packages participate in the product build.
      - uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-cache: true
          repository-cache: true

      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: Record the build host
        run: |
          . /etc/os-release && echo "distribution=$PRETTY_NAME"
          echo "kernel=$(uname -r)"
          echo "glibc=$(getconf GNU_LIBC_VERSION)"
          echo "arch=$(dpkg --print-architecture)"

      # One package per architecture, from this revision. The installed and
      # upgrade lane needs a second package built from another revision to
      # upgrade between; that is deliberately not wired here yet (see the header).
      - name: Build the candidate package
        run: ./kd build linux-package --channel staging --architecture ${{ matrix.architecture }}

      - uses: actions/upload-artifact@v4
        with:
          name: kanna-linux-${{ matrix.architecture }}
          path: .build/linux-package/out/*
          if-no-files-found: error
          retention-days: 14

  # Evidence, not a mock: `tests/linux-installed` needs apt, root, and a real
  # `systemd --user` manager. The harness's own `inspectHost()` check
  # (`tests/linux-installed/src/installedWorker.ts`) only verifies the
  # `systemctl --user` command *ran* (its exit code is non-null), which is true
  # even when it printed "Failed to connect to bus" and exited 1 — so it cannot
  # actually tell "no user manager" from "one exists". This job answers that
  # question directly on the same hosted runners the build lane already uses,
  # before any install is attempted. It does not run the installed lane itself
  # yet, and it changes nothing about the `build` job's own output.
  installed-check-prerequisites:
    if: ${{ !inputs.apt_interop_only && !inputs.prepared_pair_asset_id }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - architecture: x86_64
            runner: ubuntu-24.04
          - architecture: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    steps:
      - name: Probe systemd, session and sudo prerequisites
        run: |
          set +e
          echo "== PID 1 =="
          ps -p 1 -o comm=
          echo "== id =="
          id
          echo "== systemctl is-system-running (system manager) =="
          systemctl is-system-running
          echo "exit=$?"
          echo "== loginctl list-sessions =="
          loginctl list-sessions --no-legend
          echo "exit=$?"
          echo "== XDG_RUNTIME_DIR =="
          echo "XDG_RUNTIME_DIR=${XDG_RUNTIME_DIR:-<unset>}"
          ls -ld "/run/user/$(id -u)"
          echo "exit=$?"
          echo "== systemctl --user is-system-running (user manager) =="
          systemctl --user is-system-running
          echo "exit=$?"
          echo "== loginctl enable-linger =="
          loginctl enable-linger "$(id -un)"
          echo "exit=$?"
          loginctl show-user "$(id -un)" --property=Linger
          echo "== sudo -n true (passwordless sudo, needed for apt install) =="
          sudo -n true
          echo "exit=$?"
          set -e

  # Measured on 2026-09-10 (run 34471789591): PID 1 is `systemd`, the user
  # `systemctl --user is-system-running` reports `running` (not merely "ran"),
  # `loginctl enable-linger` succeeds and persists, and `sudo -n true` succeeds
  # on both `ubuntu-24.04` and `ubuntu-24.04-arm`. The prerequisites hold, so
  # this job does the fresh-host install itself: apt install the just-built
  # .deb (no `-dev` packages, so nothing can mask a missing `Depends`),
  # confirm every shared library the audit declared is actually owned by a
  # package and every installed executable resolves, then run the harness's
  # install-only lifecycle coverage. It does not attempt the upgrade lane —
  # that needs a second package from another source revision, still not wired
  # (§7 of the evidence doc) — and it re-checks the two critical prerequisites
  # itself rather than trusting the probe job's result, since that job is
  # intentionally never-failing evidence, not a gate.
  installed-check:
    needs: build
    strategy:
      fail-fast: false
      matrix:
        include:
          - architecture: x86_64
            runner: ubuntu-24.04
            artifact: kanna-linux-x86_64
          - architecture: arm64
            runner: ubuntu-24.04-arm
            artifact: kanna-linux-arm64
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Confirm the two critical prerequisites before attempting anything
        run: |
          sudo -n true || { echo "::error::no passwordless sudo; cannot apt install"; exit 1; }
          state="$(systemctl --user is-system-running)" || true
          case "$state" in
            running|degraded) ;;
            *) echo "::error::systemctl --user is-system-running reported '$state', not running/degraded"; exit 1 ;;
          esac

      # Same workflow run's own `build` job output for this exact head — no
      # cross-run download or extra token scope needed.
      - uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: .build/linux-package/out

      # No `-dev` packages here: a `-dev` package would satisfy the same
      # library at build time and hide a genuinely missing `Depends`. This is
      # apt resolving the shipped package's own declared dependencies, on a
      # host that never had the build's dev packages installed.
      - name: Install the built package via apt (no dev packages)
        run: |
          set -x
          # apt-get install treats a path with no leading `./` or `/` as a
          # package *name* to resolve from the repo, not a local file -- the
          # leading `./` is what tells it this is a .deb on disk.
          deb="./$(ls .build/linux-package/out/*.deb)"
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends "$deb"

      - name: Confirm every installed executable resolves and its C++ closure is static
        run: |
          set -x
          fail=0
          tree="/usr/lib/kanna-staging"
          for exe in kanna-desktop kanna-worker kanna-daemon kanna-cli kanna-mcp kanna-server kanna-task-transfer kanna-terminal-recovery; do
            echo "== $exe =="
            out="$(ldd "$tree/$exe" 2>&1)" || fail=1
            echo "$out"
            if grep -q "not found" <<<"$out"; then
              echo "::error::$exe has an unresolved dynamic dependency"
              fail=1
            fi
          done
          # Kanna's direct closure must no longer use the prototype's C++
          # exceptions. GTK/WebKit may themselves use their distro runtimes.
          for exe in "$tree"/*; do
            [ -f "$exe" ] || continue
            if readelf -d "$exe" 2>/dev/null | grep -E '\(NEEDED\).*(libc\+\+|libc\+\+abi|libunwind)\.so'; then
              echo "::error::$exe directly links a non-static C++ runtime"
              fail=1
            fi
          done
          exit $fail

      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: Compare the package audit with native readelf and installed bytes
        run: pnpm --dir tools/kd exec tsx tests/linux-product-audit.ts "$GITHUB_WORKSPACE"/.build/linux-package/out/*.deb

      # Install-only: KANNA_INSTALLED_DEB alone drives installed.e2e.test.ts.
      # upgrade.e2e.test.ts needs KANNA_INSTALLED_DEB_OLD/_NEW from two distinct
      # source revisions and is not run here (same gap as above), so it is
      # excluded rather than left to fail on a missing second artifact.
      - name: Install-only lifecycle probe (real systemd --user unit, real worker)
        working-directory: tests/linux-installed
        env:
          KANNA_INSTALLED_CHANNEL: staging
        run: |
          export KANNA_INSTALLED_DEB="$(ls "$GITHUB_WORKSPACE"/.build/linux-package/out/*.deb)"
          pnpm exec vitest run --maxWorkers=1 src/installed.e2e.test.ts

  # Controller source is this checkout. Product bytes are the independently
  # collected, stamped pair, pinned in the committed evidence manifests.
  prepared-upgrade:
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.prepared_pair_asset_id != '' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - architecture: x86_64
            runner: ubuntu-24.04
          - architecture: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Fetch and verify the exact prepared pair (no rebuild)
        env:
          PAIR_ASSET_ID: ${{ inputs.prepared_pair_asset_id }}
          GH_TOKEN: ${{ github.token }}
          PAIR_SHA256: ${{ inputs.prepared_pair_sha256 }}
        run: |
          mkdir -p .tmp/prepared-upgrade
          [[ "$PAIR_SHA256" =~ ^[0-9a-f]{64}$ ]]
          python3 tests/linux-installed/fetch-prepared-pair.py .tmp/prepared-upgrade/pair.tar
          printf '%s  %s\n' "$PAIR_SHA256" .tmp/prepared-upgrade/pair.tar | sha256sum --check --strict
          python3 tests/linux-installed/verify-prepared-pair.py .tmp/prepared-upgrade/pair.tar .tmp/prepared-upgrade/packages
      - name: Confirm real host prerequisites
        run: |
          sudo -n true
          state="$(systemctl --user is-system-running)" || true
          case "$state" in running|degraded) ;; *) exit 1 ;; esac
          . /etc/os-release
          test "$VERSION_ID" = 24.04
          getconf GNU_LIBC_VERSION
          uname -a
          sudo apt-get update
      - run: pnpm --filter @kanna/linux-installed... --filter @kanna/kd... install --frozen-lockfile --ignore-scripts
      - name: Canonical installed and two-version upgrade gate
        env:
          KANNA_INSTALLED_EVIDENCE_DIR: ${{ github.workspace }}/.tmp/prepared-upgrade/evidence
          TEST_ARCH: ${{ matrix.architecture }}
        run: |
          export TMPDIR="$GITHUB_WORKSPACE/.tmp/prepared-upgrade/tmp"
          mkdir -p "$TMPDIR"
          deb_arch=amd64
          if [ "$TEST_ARCH" = arm64 ]; then deb_arch=arm64; fi
          ./kd test linux-installed --channel staging \
            --old-artifact "$GITHUB_WORKSPACE/.tmp/prepared-upgrade/packages/kanna-staging_0.2.0~staging.1-1_${deb_arch}.deb" \
            --new-artifact "$GITHUB_WORKSPACE/.tmp/prepared-upgrade/packages/kanna-staging_0.2.0~staging.2-1_${deb_arch}.deb"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: prepared-upgrade-${{ matrix.architecture }}-evidence
          # Keep private runtime files/DBs local: upload only structured API
          # evidence, assertions and owned unit logs, never generated credentials.
          path: |
            .tmp/prepared-upgrade/evidence/*.jsonl
            .tmp/prepared-upgrade/evidence/vitest-result.json
            .tmp/prepared-upgrade/evidence/*/journal.log
            .tmp/prepared-upgrade/evidence/*/unit.txt
          if-no-files-found: error
          retention-days: 14
