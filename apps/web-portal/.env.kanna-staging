# Public account-portal browser configuration for the staging Firebase project
# (kanna-staging). `./kd cloud deploy staging --portal` reads this file as a
# layer beneath its own environment, so an operator export still wins.
#
# These are NOT secrets. Vite compiles every value here into the portal bundle
# and Firebase Hosting serves it to every visitor; each one below was read back
# out of the live bundle at https://kanna-staging-account.web.app. The Stripe
# SECRET key and STRIPE_WEBHOOK_SECRET live in the Functions secret store
# (`firebase functions:secrets:set`) and must never appear in a repository file.
#
# Vite does not load this file: it reads `.env.[mode]`, and the portal build
# runs in mode "production". `kd` is the only reader.
KANNA_WEB_PORTAL_FIREBASE_API_KEY=AIzaSyCRsov6oQu8Fg0clB2mdB5RgwM8GGwCQXk
KANNA_WEB_PORTAL_FIREBASE_APP_ID=1:1073113006696:web:3bca4e7586f5587e1c71dd
# Stripe TEST publishable key, sandbox account acct_1Swy1rI0Oqa4EKBj
# (`docs/specs/accounts-and-billing.md`). Staging must never carry the live key.
KANNA_WEB_PORTAL_STRIPE_PUBLISHABLE_KEY=pk_test_51Swy1rI0Oqa4EKBjeA3ROCoK7MQeHXUO5MPZeVE3LaKw7zYAzzjEg4MMUdPv6NE5OwXcdoZTU5JpS2EXMNQ3Ijle00Rdq6Kv57

# KANNA_WEB_PORTAL_FIREBASE_AUTH_DOMAIN, _FIREBASE_FUNCTIONS_REGION and
# _CLOUD_PRICE are deliberately unset: the resolver's defaults
# (kanna-staging.firebaseapp.com, us-central1, $5/month) are what the live
# bundle already carries.
